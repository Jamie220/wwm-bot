name: Cron Test

on:
  schedule:
    - cron: "3,13,23,33,43,53 * * * *"
      timezone: "Australia/Melbourne"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show current time
        run: date

name: Cron Test

on:
  schedule:
    - cron: "7,17,27,37,47,57 * * * *"
      timezone: "Australia/Melbourne"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show current time
        run: date
